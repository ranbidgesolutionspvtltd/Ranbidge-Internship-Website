{
  "name": "ranbidge-internship-website",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "cd \"Ranbidge Intern Website\" && npm install && npx vite build",
    "install-deps": "cd \"Ranbidge Intern Website\" && npm install"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
